# Server Configuration
PORT=8080
ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_ROOT_PASSWORD=

# JWT Configuration
JWT_SECRET_KEY=
JWT_REFRESH_SECRET=
JWT_EXPIRY_HOURS=24

# MercadoPago Configuration
MERCADOPAGO_ACCESS_TOKEN=
MERCADOPAGO_PUBLIC_KEY=
# Webhook secret from MercadoPago dashboard: Your integrations > app > Webhooks > Secret key
# Leave empty in development to skip signature validation (warning logged).
# REQUIRED in production.
MERCADOPAGO_WEBHOOK_SECRET=

# API Configuration
API_BASE_URL=http://localhost:8080

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=soyejemplo@ejemplo_valido.com
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

